Mean CMS combined backend + Angular frontend.

Backend: ./backend
Frontend: ./frontend

Run backend:
  cd backend
  npm install
  npm run dev

Run frontend:
  cd frontend
  npm install
  npm start

Make sure backend is running on http://localhost:4000 or update src/environments/environment.ts in the frontend.
